{
  "name": "chromemcp",
  "version": "0.1.0",
  "private": true,
  "description": "User-wide MCP server stack. Runs Playwright MCP as a long-running HTTP/SSE service connected to ChromeMCP's bridged Chrome via CDP, so opted-in clients and repositories can share the same browser session.",
  "engines": {
    "node": ">=18.18"
  },
  "dependencies": {
    "@playwright/mcp": "0.0.76"
  },
  "overrides": {
    "playwright-core": "1.61.0-alpha-1781023400000",
    "playwright": "1.61.0-alpha-1781023400000"
  },
  "scripts": {
    "start": "bash ./start.sh",
    "stop": "bash ./stop.sh"
  }
}
